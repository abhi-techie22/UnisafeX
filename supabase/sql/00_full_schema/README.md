# Full Schema SQL

`000_complete_schema.sql` is the full UniSafeX schema snapshot.

Use this for a fresh Supabase project or when you intentionally want to rebuild
the database schema from one complete SQL file.

For an existing project with data, prefer the folder-wise patch order in:

`../99_apply_order/apply_order.md`
