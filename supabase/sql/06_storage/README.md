# Storage SQL

Contains public tourism media storage setup.

Tourism media is public by design because destination photos are public app
content. User documents must use the private `user-documents` bucket created in
`07_security_hardening/002_launch_security_hardening.sql`.
