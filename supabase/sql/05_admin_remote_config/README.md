# Admin Remote Config SQL

Contains app settings, feature flags, home banners, travel alerts,
notifications, users/profile views, and currency-rate settings.

Only admins should write these tables. Normal users and guests should only read
public active content where the policies explicitly allow it.
