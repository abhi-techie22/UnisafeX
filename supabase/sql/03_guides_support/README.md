# Guides And Support SQL

Contains guide requests, guide assignment/booking, saved guide profiles, admin
team helpers, and support tickets.

User rows must remain isolated by `auth.uid()`. Admin access must be enforced
by database policies, not frontend checks alone.
