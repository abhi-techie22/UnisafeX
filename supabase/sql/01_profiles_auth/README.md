# Profiles And Auth SQL

Contains profile table RLS, auth trigger sync, email sync, and app-side auth
configuration rows.

Run these before guide, booking, admin, and support SQL because many later
features depend on authenticated users and profile rows.
